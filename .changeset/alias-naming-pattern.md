---
"oh-my-space": minor
---

Allow `oms.yaml` aliases to include underscores and `@`, while keeping aliases lowercase and still requiring the first character to be an ASCII lowercase letter or digit.
