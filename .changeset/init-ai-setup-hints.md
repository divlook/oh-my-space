---
"oh-my-space": patch
---

`oms init` now prints optional AI-setup hints after scaffolding `oms.yaml`, pointing to `oms agent install` and `oms skills`. Additive output only — no new command, flag, or behavior change.
