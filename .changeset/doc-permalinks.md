---
"oh-my-space": patch
---

Print migration-doc hints as clickable GitHub permalinks pinned to the build commit, instead of repo-relative paths, so they are clickable in the terminal and never break when files move on the default branch.
