# Tasks: add-temporary-worktree

## 1. Spike — worktree mechanics under a submodule gitdir

- [x] 1.1 Build a scratch workspace (root Git repository plus one submodule whose gitdir is absorbed under `.git/modules/oms/<alias>`) and verify `git -C oms/<alias> worktree add` from both a branch and a detached HEAD: record where the tree's `.git` file points (relative or absolute) and where the administrative worktree metadata lands, and verify that `git --git-dir=<submodule-gitdir> worktree list --porcelain` enumerates satellite trees when probed without the canonical working tree, since the inventory locates gitdirs directly.
- [x] 1.2 Verify that moving the workspace root breaks the tree link and that `git worktree repair` restores it, and record the `git worktree list --porcelain` output shape for healthy, broken, and prunable entries. Append a short "Spike findings" note to `design.md` correcting D1/D6 if observed behavior differs.

## 2. Core tree module

- [x] 2.1 Add the `.oms-tree` constants (directory name, local-exclude entry and comment) and create `scripts/lib/tree-ops.ts` with managed-tree path helpers and `listManagedTrees(repoRoot)` built on the union of a filesystem scan of `.oms-tree/<alias>/<task>` entries and `git worktree list --porcelain` from every locatable submodule gitdir, with broken-link detection, foreign-directory error entries, null `branch`/`head`/`dirty` for unreadable state, and untracked-inclusive dirty semantics, following the injectable-runner pattern of `scripts/lib/git.ts`.
- [x] 2.2 Implement `runTreeAdd`: shared alias preparation without detached-HEAD resolution (the branch starts at the current HEAD commit and the canonical checkout is untouched), task-name validation via `git check-ref-format` with the no-separator rule, duplicate tree-path refusal, `--from` semantics (create at start point, resume an existing branch, fail on `--from` with an existing branch), worktree creation, the idempotent `.git/info/exclude` append, and a one-line success confirmation naming the alias and the task.
- [x] 2.3 Implement `runTreeRemove`: dirty refusal without `--force`, forced removal that still preserves the branch, prune path for broken trees (including a vanished submodule gitdir), foreign-directory cleanup (empty removed, non-empty refused without `--force`), refusal when the cwd is inside the target tree, interactive alias-then-task selection for omitted arguments with a non-interactive usage error, empty-parent cleanup through the `.oms-tree/` root (exclude entry kept), the resume/delete guidance message, and the unknown-tree failure.
- [x] 2.4 Implement `runTreeList`: alias, task, branch, dirty, and broken state per inventory entry, including foreign-directory error entries, with the empty state reporting none and exiting 0.

## 3. CLI wiring and guard

- [x] 3.1 Register the `tree add`, `tree list`, and `tree remove` commands in `scripts/oms.ts` (including the top-level command set used for unknown-command detection) and add their help text in `scripts/lib/help.ts`, naming the `.oms-tree/` location, the no-root-commit guarantee, and usage examples; bare `oms tree` prints help. Keep the top-level command description free of the word "worktree" — the `cli-scaffold` help contract forbids it in the top-level help — with the Git-worktree detail in `oms tree --help`, and extend that contract to expect `tree`.
- [x] 3.2 Add the shared tree-cwd guard (cwd under the resolved root's `.oms-tree/` fails with guidance before alias resolution or mutation) and wire it into `oms commit`, `oms record`, bare `oms branch` (before its action selector) plus `oms branch switch|checkout|list|delete`, and `oms fetch|pull|push`; keep `oms status`, `oms doctor`, and `oms tree` usable from inside a tree.

## 4. Integration with existing commands

- [x] 4.1 Add the `oms unsync` live-tree preflight: refuse while any inventory entry exists for a selected alias — healthy, broken, or foreign — including with `--force`, naming the entries and `oms tree remove`.
- [x] 4.2 Extend `scripts/lib/status.ts`: `oms status --json` gains the top-level `trees` array with the spec fields, empty when none, narrowed by alias filters, with non-null `error` and null `branch`/`head`/`dirty` for broken entries and non-null `error` for foreign directories; the human output lists trees when any exist; update `oms status --help` to document `trees` under `schemaVersion` 1.
- [x] 4.3 Extend `scripts/lib/doctor.ts` to report broken managed trees with `git worktree repair` as the remediation, and to warn when `.oms-tree/` exists but the root's local exclude entry is missing, naming `oms tree add` as the command that re-asserts it.

## 5. Skills and documentation

- [x] 5.1 Extend `skills/oms-workspace/SKILL.md` with the managed-tree scope instructions (Git directly inside trees, push and PR from the tree, reflect with `oms pull` then `oms record`) and bump its `metadata.version` minor per the bump policy; leave the guardrail kernel untouched.
- [x] 5.2 Extend `skills/oms-branch/SKILL.md` with starting task work via `oms tree add <alias> <task>` and bump its `metadata.version` minor per the bump policy.
- [x] 5.3 Update `docs/commands.md` (command map row plus tree command sections), `docs/how-oms-works.md` (managed-tree model, machine-local exclusion, PR-centric reflection flow), and `docs/ai-coding-tools.md` (agent workflow inside trees), and add a minor changeset.

## 6. Tests and verification

- [x] 6.1 Add unit tests for the pure tree-ops decisions, driven by a queued mock runner (`tests/unit/git.test.ts` pattern) and fed the porcelain shapes the spike records: porcelain parsing for healthy, broken, and prunable entries; the filesystem-scan ∪ porcelain union with foreign-directory classification and undeclared-alias retention; broken-entry null `branch`/`head`/`dirty`; untracked-inclusive dirty derivation; task-name validation; and the exclude-entry idempotence decision.
- [x] 6.2 Add blackbox tree-lifecycle journeys in `tests/cli-tree.contracts.js`, sharded across `tests/cli-tree-a.test.js` and `tests/cli-tree-b.test.js` via `OMS_TEST_SHARD_NAMES` (the preparation-sharding pattern), each journey reusing one workspace fixture: add from canonical HEAD, add from a detached canonical HEAD without touching it, `--from`, resume, `--from` conflict, duplicate path including a foreign directory, offline creation, one-line success output, exclude-entry idempotence, remove clean/dirty/force/prune, remove from inside the target tree, foreign-directory cleanup empty and non-empty, empty-parent and `.oms-tree/`-root cleanup, and list states including foreign directories.
- [x] 6.3 Add blackbox cross-command contracts in the same contracts file: the tree-cwd guard on representative paths only — one explicit-alias and one omitted-alias command, bare `oms branch` before its action selector, and one manage-ops command, each asserting no mutation before refusal — one read-only command inside a tree, unsync inventory-entry refusal with and without `--force` including broken and foreign entries, the `trees` array in `oms status --json` (alias filtering, broken-link error with null fields, foreign-directory entries), and doctor's broken-tree report and missing-exclude-entry warning.
- [x] 6.4 Register the new contracts and owners in `tests/test-inventory.json` with a process-boundary rationale for every blackbox entry, add the tree owner files to the blackbox `ownerOrder`, and keep `npm run test:inventory` green.
- [x] 6.5 Run the full suite (`npm test`) and `openspec validate add-temporary-worktree --strict`, confirm the canonical suite stays within the recorded performance budget (median `npm test` at most 60 seconds, no run over 75 seconds, per `openspec/specs/test-execution/spec.md`), and fix any fallout so all are green. Functional tests and validation are green; `evidence/suite-timings.json` records that the current machine could not reproduce the historical timing budget (existing unchanged owners are ~35% slower), the tree contracts were reduced from 18 to 14, and their measured incremental blackbox wall cost is ~3 seconds. Re-run the three-run benchmark on an idle baseline-equivalent M2 for final performance sign-off.
