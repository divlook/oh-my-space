# common

# osx
.DS_Store

# node
node_modules/

dist/
.test-dist/
.oms-verification.json
# claude
.claude/plans/

# ci
# Verification marker written by the CI workflow; also left behind when the
# fingerprint and marker steps are reproduced locally.
.ci-verified
