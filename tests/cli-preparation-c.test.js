process.env.OMS_TEST_SHARD_NAMES = JSON.stringify([
  "detached HEAD attaches safely, offers a moving choice, and fails without intent",
  "preparation defaults follow selection scope and skip remains successful",
  "every preparing command refuses partial registration with repair guidance",
  "detached HEAD creates a branch through the queued text prompt",
]);
await import("./cli-preparation.contracts.js");
