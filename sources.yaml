# yaml-language-server: $schema=./sources.schema.json

# Replace these example entries with the repositories you want to analyze.
# Each `alias` becomes the directory name under `sources/<alias>/`.
repos:
  - alias: example
    url: git@github.com:OWNER/REPO.git
    # branch: develop  # optional — clone a non-default branch
